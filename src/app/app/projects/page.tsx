import Link from "next/link";
import { getProjects } from "@/features/projects/queries";
import { ProjectCard } from "@/features/projects/components/project-card";
export default async function ProjectsPage() { const projects = await getProjects(); return <main className="min-h-screen bg-[#090909] px-6 py-12 text-zinc-100"><section className="mx-auto max-w-4xl"><div className="flex items-end justify-between gap-4"><div><Link href="/app" className="text-sm text-cyan-300">BuildFlow</Link><h1 className="mt-6 text-3xl font-semibold">프로젝트</h1></div><Link href="/app/projects/new" className="border border-cyan-400/50 bg-cyan-400/10 px-4 py-3 text-sm text-cyan-200">새 프로젝트</Link></div>{projects.length === 0 ? <div className="mt-10 border border-zinc-800 p-8"><p className="text-zinc-300">아직 프로젝트가 없습니다.</p><p className="mt-2 text-sm text-zinc-500">첫 프로젝트를 만들어 목표를 정리해보세요.</p></div> : <div className="mt-10 grid gap-4 md:grid-cols-2">{projects.map((project) => <ProjectCard key={project.id} project={project} />)}</div>}</section></main>; }
