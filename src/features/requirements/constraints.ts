import type { ConstraintAssessment, Requirement } from "./types";
export function assessConstraints(requirement: Requirement): ConstraintAssessment[] { const results: ConstraintAssessment[] = []; results.push(requirement.automationLevel === "high" ? { level: "partial_automatic", reason: "외부 계정과 실행 권한 확인이 필요해 일부 단계는 사용자 승인이 필요합니다.", requiresUserAction: true } : { level: "automatic", reason: "현재 입력 기준으로 설계와 안내를 자동화할 수 있습니다.", requiresUserAction: false }); if (requirement.budget === "free") results.push({ level: "partial_automatic", reason: "무료 플랜과 사용량 제한을 먼저 확인해야 합니다.", requiresUserAction: true }); if (requirement.category === "unknown") results.push({ level: "user_required", reason: "목표를 더 구체화해야 적합한 시스템을 설계할 수 있습니다.", requiresUserAction: true }); return results; }
