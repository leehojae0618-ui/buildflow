export type DeploymentState = "PREPARING" | "BUILDING" | "DEPLOYING" | "WAITING_FOR_PROVIDER" | "WAITING_FOR_USER" | "VERIFYING" | "RECOVERING" | "READY" | "READY_WITH_WARNINGS" | "BLOCKED" | "FAILED" | "CANCELLED";
export type BuildEstimate = { estimatedTotalMinutes: number; estimatedActiveBuildMinutes: number; estimatedProviderWaitMinutes: number; estimatedUserActionMinutes: number; estimatedMonthlyCostCents: number; estimatedInitialCostCents: number; automationPercentage: number; userActionCount: number; approvalCount: number; confidence: number; risk: number };
export type CompletionReport = { state: DeploymentState; productionReady: boolean; serviceUrl?: string; adminUrl?: string; operatingCostCents: number; backupStatus: "UNKNOWN" | "READY"; monitoringStatus: "UNKNOWN" | "READY"; healthCheckPassed: boolean; requiredVerificationPassed: boolean; requiredFunctionTestsPassed: boolean; criticalErrors: number; nextRecommendations: string[]; warnings: string[]; actualMinutes?: number; userInterruptionCount: number };
export type DeploymentSession = { id: string; projectId: string; userId: string; state: DeploymentState; currentStage: string; completedStages: string[]; estimate: BuildEstimate; report: CompletionReport; retryCount: number; automaticRecoveryCount: number; createdAt: string; updatedAt: string };
