export const statusLabel: Record<string, string> = { draft: "작성 중", active: "진행 중", archived: "보관됨", pending: "준비 중", completed: "완료", failed: "실패", selected: "선택됨", in_progress: "진행 중", setup: "준비 중", ready: "준비 완료" };
export function truncate(value: string | null | undefined, length = 100) { const text = value?.trim() ?? ""; return text.length > length ? `${text.slice(0, length).trim()}…` : text; }
export function relativeDate(value: string, now = new Date("2026-07-15T12:00:00Z")) { const diff = Math.max(0, now.getTime() - new Date(value).getTime()); const minutes = Math.floor(diff / 60000); if (minutes < 1) return "방금 전"; if (minutes < 60) return `${minutes}분 전`; const hours = Math.floor(minutes / 60); if (hours < 24) return `${hours}시간 전`; if (hours < 48) return "어제"; return new Intl.DateTimeFormat("ko-KR", { dateStyle: "medium" }).format(new Date(value)); }
