import { describe, expect, it } from "vitest";
import { commandFromPlan, validateProvisioningCommand } from "./commands";
import { githubAdapter, providerAdapters } from "./adapters";
describe("secure provider provisioning contract", () => { it("creates and validates commands from approved scope", async () => { const command = commandFromPlan("p", "github", "CREATE_REPOSITORY", { name: "demo", visibility: "private" }, "scope-v1"); expect(validateProvisioningCommand(command)).toEqual([]); expect((await githubAdapter.validateCommand(command)).status).toBe("PREPARED"); }); it("rejects secret fields and pauses without credential", async () => { const command = { id: "x", provider: "vercel" as const, kind: "DEPLOY" as const, projectId: "p", payload: { token: "never" }, approvalScope: "scope", estimatedCostCents: 0, reversible: true }; expect(validateProvisioningCommand(command)).toContain("COMMAND_SECRET_FIELD"); const safeCommand = { ...command, payload: { deployment: "preview" } }; const result = await providerAdapters.vercel.executeCommand(safeCommand, { referenceId: "credential-ref-vercel", resolve: async () => null }); expect(result.status).toBe("WAITING_FOR_USER"); }); it("sanitizes result without response body or secrets", () => { const result = githubAdapter.sanitizeResult({ ok: false, status: "FAILED", safeCode: "X", retryable: false, resourceId: "r" }); expect(JSON.stringify(result)).not.toContain("token"); }); });
